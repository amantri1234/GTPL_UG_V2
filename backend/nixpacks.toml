# Nixpacks configuration for Railway deployment
# Alternative to Dockerfile

[phases.build]
cmds = ["./gradlew shadowJar --no-daemon -x test"]

[phases.setup]
nixPkgs = ["jdk17"]

[start]
cmd = "java -Dserver.port=${PORT} -jar build/libs/gtpl-ug-system.jar"
